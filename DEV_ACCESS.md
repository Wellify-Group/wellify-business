# 🔓 Доступ к дашбордам без регистрации (Режим разработки)

Для быстрого доступа к страницам директора и сотрудника без регистрации и входа используй режим разработки.

## 🚀 Быстрый доступ

### Вариант 1: Через специальную страницу (рекомендуется)

Открой в браузере:

- **Директор:** `http://localhost:3000/dev?role=director`
- **Менеджер:** `http://localhost:3000/dev?role=manager`
- **Сотрудник:** `http://localhost:3000/dev?role=employee`

Страница автоматически:
1. Создаст мок-пользователя с нужной ролью
2. Установит тестовые данные (локации, сотрудники)
3. Перенаправит на соответствующий дашборд

### Вариант 2: Прямой доступ с параметром `?dev=true`

Добавь `?dev=true` к любому URL дашборда:

- `http://localhost:3000/dashboard/director?dev=true`
- `http://localhost:3000/dashboard/employee?dev=true`
- `http://localhost:3000/dashboard/manager?dev=true`

**Важно:** Для прямого доступа нужно сначала установить мок-пользователя через `/dev?role=...`

### Вариант 3: Через переменную окружения

Установи переменную окружения:

```bash
# В .env.local
NEXT_PUBLIC_DEV_MODE=true
```

После этого все маршруты `/dashboard/*` будут доступны без авторизации.

## 📝 Что делает режим разработки

1. **Middleware пропускает проверку авторизации** - если `NODE_ENV=development` или есть параметр `?dev=true`
2. **Создаётся мок-пользователь** с нужной ролью
3. **Устанавливаются тестовые данные:**
   - Для директора/менеджера: 2 тестовые локации, 1 сотрудник
   - Для сотрудника: 1 тестовая локация

## ⚙️ Настройка

### Мок-данные

Мок-данные создаются автоматически на странице `/dev`. Если нужно изменить их, отредактируй `app/dev/page.tsx`.

### Отключение режима разработки

1. Удали `NEXT_PUBLIC_DEV_MODE=true` из `.env.local`
2. Или установи `NODE_ENV=production`
3. Или не используй параметр `?dev=true` в URL

## 🔒 Безопасность

**⚠️ ВАЖНО:** Режим разработки работает только в:
- `NODE_ENV=development`
- Или с явным параметром `?dev=true` в URL
- Или с `NEXT_PUBLIC_DEV_MODE=true`

В production (`NODE_ENV=production`) режим разработки **автоматически отключается**.

## 🐛 Troubleshooting

### Страница не открывается

1. Убедись, что сервер запущен: `npm run dev`
2. Проверь, что middleware не блокирует доступ
3. Попробуй очистить cookies и localStorage

### Нет данных на дашборде

1. Убедись, что открыл `/dev?role=director` (или другую роль) перед переходом на дашборд
2. Проверь, что мок-пользователь установлен в store (открой DevTools → Application → Local Storage)

### Middleware всё ещё требует авторизацию

1. Проверь, что `NODE_ENV=development` или есть `?dev=true` в URL
2. Убедись, что переменная `NEXT_PUBLIC_DEV_MODE` не установлена в `false`

## 📚 Дополнительно

- Middleware: `middleware.ts`
- Страница доступа: `app/dev/page.tsx`
- Store с мок-данными: `lib/store.ts`

---

**Готово!** Теперь ты можешь быстро тестировать дашборды без регистрации! 🎉
