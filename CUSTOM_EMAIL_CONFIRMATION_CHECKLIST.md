# Чеклист настройки кастомного подтверждения email

## ✅ Что уже сделано в коде

1. ✅ Создан эндпоинт `/api/auth/send-custom-email-confirmation` для отправки кастомных писем
2. ✅ Создан эндпоинт `/api/auth/confirm-email` для обработки подтверждения по кастомному токену
3. ✅ Изменена регистрация - отключена стандартная отправка писем Supabase
4. ✅ Проверка email через `/api/auth/check-email-confirmed` работает (проверяет `email_verified` в profiles)
5. ✅ Polling в регистрации использует правильный эндпоинт

## 📋 Что нужно сделать в Supabase Dashboard

### Для DEV проекта:

1. **Выполнить SQL миграции:**
   - Откройте Supabase Dashboard → ваш DEV проект
   - Перейдите в SQL Editor
   - Выполните содержимое файла `supabase/migrations/013_create_email_verifications.sql`
   - Выполните содержимое файла `supabase/migrations/014_create_confirm_email_function.sql`

2. **Настройки Authentication (опционально):**
   - Перейдите в Authentication → Settings
   - Убедитесь, что "Enable email confirmations" включено (для безопасности)
   - Но мы не используем стандартные письма Supabase, поэтому это не критично

### Для PRODUCTION (main) проекта:

То же самое, что для DEV, но в PRODUCTION Supabase проекте.

## 🔍 Дополнительные проверки

### 1. Проверить переменные окружения в Railway

Убедитесь, что в `wellify-auth-service` установлены:
- ✅ `RESEND_API_KEY` - ключ Resend API
- ✅ `EMAIL_FROM` - адрес отправителя
- ✅ `APP_BASE_URL` - URL Railway сервиса (разные для dev и production)
- ✅ `SUPABASE_URL` - URL Supabase проекта
- ✅ `SUPABASE_SERVICE_ROLE_KEY` - Service Role Key

### 2. Проверить работу триггера синхронизации

Убедитесь, что триггер `sync_profile_email_verified` существует и работает:
- Он должен автоматически синхронизировать `profiles.email_verified` с `auth.users.email_confirmed_at`
- Это запасной вариант на случай, если функция `confirm_user_email` не сработает

### 3. Протестировать полный flow

1. Зарегистрируйте нового пользователя
2. Проверьте, что письмо пришло на почту
3. Кликните на ссылку в письме
4. Убедитесь, что открылась страница "E-mail подтверждён"
5. Кликните еще раз - должна открыться страница "E-mail уже подтверждён"
6. Проверьте в Supabase, что:
   - `auth.users.email_confirmed_at` заполнен
   - `profiles.email_verified = true`
   - `email_verifications.verified_at` заполнен

## ⚠️ Важные моменты

1. **Стандартные письма Supabase больше не используются** - мы отправляем письма через Resend
2. **emailRedirectTo не передается** - чтобы Supabase не отправлял стандартные письма
3. **Проверка email** - идет через `profiles.email_verified`, который обновляется напрямую при подтверждении
4. **Токены действительны 24 часа** - после истечения нужно будет запросить новое письмо

## 🔧 Если что-то не работает

1. Проверьте логи Railway - должны быть логи из `/api/auth/confirm-email`
2. Проверьте логи Supabase - должны быть вызовы функции `confirm_user_email`
3. Проверьте таблицу `email_verifications` - должна быть запись с токеном
4. Проверьте `profiles.email_verified` - должен быть `true` после подтверждения
5. Проверьте `auth.users.email_confirmed_at` - должен быть заполнен после подтверждения

