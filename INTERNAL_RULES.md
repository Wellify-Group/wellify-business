# Внутренние правила и документация WELLIFY Business

Этот файл содержит правила работы системы, описание процессов и логики, которые должны соблюдаться при разработке и исправлении багов.

---

## 📋 СОДЕРЖАНИЕ

1. [Процесс регистрации директора](#процесс-регистрации-директора)
2. [Правила работы с темами (Light/Dark)](#правила-работы-с-темами)
3. [Правила центрирования контента](#правила-центрирования-контента)
4. [Правила работы с переводами (i18n)](#правила-работы-с-переводами)
5. [Правила работы с дизайн-системой](#правила-работы-с-дизайн-системой)

---

## 🔐 ПРОЦЕСС РЕГИСТРАЦИИ ДИРЕКТОРА

### Общее описание

Регистрация директора проходит в 4 шага:
1. **Основные данные** - личная информация и пароль
2. **E-mail** - подтверждение рабочего e-mail
3. **Telegram** - верификация телефона через Telegram бота
4. **Завершение** - переход в дашборд

### Шаг 1: Основные данные

**Файл:** `app/register/RegisterDirectorClient.tsx` → `renderStep1()`

**Поля формы:**
- Имя (`firstName`) - обязательное
- Отчество (`middleName`) - опциональное
- Фамилия (`lastName`) - обязательное
- Дата рождения (`birthDate`) - обязательное
- Пароль (`password`) - минимум 8 символов, обязательное
- Подтверждение пароля (`passwordConfirm`) - должно совпадать с паролем

**Валидация:**
- Имя и фамилия обязательны
- Дата рождения обязательна
- Пароль минимум 8 символов
- Пароль и подтверждение должны совпадать

**Переход на шаг 2:**
- При нажатии "Далее" выполняется `handleNextFromStep1()`
- Если валидация пройдена → `setStep(2)` и `setMaxStepReached(2)`

---

### Шаг 2: Подтверждение E-mail

**Файл:** `app/register/RegisterDirectorClient.tsx` → `renderStep2()`

**Процесс:**

1. **Ввод e-mail:**
   - Пользователь вводит рабочий e-mail
   - Валидация формата e-mail

2. **Отправка письма:**
   - При нажатии "Далее" вызывается `handleSendEmailLink()`
   - Выполняется `supabase.auth.signUp()` с:
     - `email` - введенный e-mail
     - `password` - пароль из шага 1
     - `options.data` - личные данные (first_name, last_name, middle_name, full_name, birth_date, locale)
     - `options.emailRedirectTo` - `/email-confirmed`

3. **Polling подтверждения:**
   - После отправки письма запускается `useEffect` с polling
   - Каждые 1.5 секунды проверяется `/api/auth/check-email-confirmed?email=...`
   - При подтверждении:
     - `setEmailStatus("verified")`
     - `setEmailVerified(true)`
     - `setStep(3)` - переход на шаг Telegram
     - `setMaxStepReached(3)`

**Важно:**
- После `signUp` сохраняются `registeredUserId` и `registeredUserEmail`
- Эти данные нужны для шага 3 (Telegram верификация)

---

### Шаг 3: Верификация Telegram

**Файл:** `app/register/RegisterDirectorClient.tsx` → `renderStep3()`

**Компонент:** `app/register/TelegramVerificationStep.tsx`

**Процесс:**

1. **Создание сессии:**
   - При монтировании компонента создается Telegram сессия через `/api/telegram/create-session`
   - Генерируется QR-код и ссылка для Telegram бота

2. **Polling статуса:**
   - Компонент опрашивает `/api/telegram/session-status/[sessionToken]`
   - При успешной верификации вызывается `onVerified(phone)`

3. **Обновление профиля:**
   - Telegram бот обновляет в БД:
     - `phone` - номер телефона пользователя
     - `telegram_verified` - устанавливается в `true`
   - Это происходит на стороне бота, не в клиентском коде

4. **Переход на шаг 4:**
   - При вызове `handleTelegramVerified()`:
     - Сохраняется `verifiedPhone` (если передан)
     - `setStep(4)` - переход на финальный шаг
     - `setMaxStepReached(4)`

**Важно:**
- Обновление данных в БД происходит асинхронно на стороне бота
- Может быть задержка между завершением верификации и обновлением БД

---

### Шаг 4: Завершение регистрации

**Файл:** `app/register/RegisterDirectorClient.tsx` → `renderStep4()`

**Процесс:**

1. **Инициализация шага 4:**
   - При переходе на шаг 4 **сразу очищаются все ошибки**: `setRegisterError(null)`
   - Это предотвращает показ старых ошибок

2. **Автоматическая проверка данных:**
   - Запускается `useEffect` с polling (только если `step === 4`)
   - Первая проверка через **1.5 секунды** (дает время БД обновиться)
   - Затем проверка каждые **2 секунды**
   - Используется `/api/auth/load-profile` с `credentials: 'include'`

3. **Обработка ошибок при проверке:**
   - **401 (Unauthorized):** Не показывается ошибка, просто ждем (сессия еще не готова)
   - **Временные проблемы сети:** Не показывается ошибка, просто ждем
   - **Другие ошибки:** Логируются в консоль, но не показываются пользователю
   - **Нет сессии:** Не показывается ошибка, просто ждем

4. **Проверка условий:**
   - Проверяются два условия:
     - `phone` должен быть заполнен (не пустой): `profile?.phone && profile.phone.trim() !== ""`
     - `telegram_verified` должен быть `true` (или строка `"true"`, или число `1`)

5. **Отображение статуса:**
   - **Если данные готовы:**
     - `setRegisterError(null)` - убирается любое сообщение
     - Polling останавливается
     - Можно нажать "Перейти в дашборд"
   - **Если данные не готовы:**
     - Показывается **информационное сообщение** (синий цвет): "Ожидание подтверждения данных Telegram..."
     - Это НЕ ошибка, а информационное сообщение
     - Polling продолжается

6. **Переход в дашборд:**
   - При нажатии "Перейти в дашборд" вызывается `finishRegistration()`
   - Функция проверяет:
     - Наличие сессии пользователя (или восстанавливает через `signInWithPassword`)
     - Загружает профиль из БД через `/api/auth/load-profile` с `credentials: 'include'`
     - **Обработка 401:** Показывается ошибка "Сессия истекла. Пожалуйста, войдите заново."
     - Проверяет два условия:
       - `phone` заполнен и не пустой
       - `telegram_verified === true` (или строка `"true"`, или число `1`)
     - Если оба условия выполнены → `router.push("/dashboard/director")`
     - Если нет → показывается соответствующая ошибка

**КРИТИЧЕСКИ ВАЖНО:**
- Переход в дашборд возможен ТОЛЬКО если выполнены оба условия
- Никаких дополнительных проверок не требуется
- Если условия не выполнены - показывается ошибка, редиректа нет
- **Сообщение "Ожидание подтверждения данных Telegram..." - это НЕ ошибка**, а информационное сообщение (синий цвет)
- **Ошибки показываются только при реальных проблемах** (красный цвет)

---

### API Endpoints, используемые в регистрации

1. **`/api/auth/check-email-confirmed`**
   - Проверяет статус подтверждения e-mail
   - Используется для polling на шаге 2

2. **`/api/auth/load-profile`**
   - Загружает полный профиль пользователя из БД
   - Обходит RLS через admin клиент
   - Возвращает: `phone`, `telegram_verified`, `phone_verified` и другие поля
   - Используется на шаге 4 для проверки готовности данных

3. **`/api/telegram/create-session`**
   - Создает сессию для Telegram верификации
   - Возвращает `sessionToken` и `telegramLink`

4. **`/api/telegram/session-status/[sessionToken]`**
   - Проверяет статус Telegram сессии
   - Используется для polling в `TelegramVerificationStep`

---

### Поля в таблице `profiles`, важные для регистрации

- `id` (uuid) - ID пользователя (1:1 с `auth.users.id`)
- `phone` (text) - номер телефона (заполняется Telegram ботом)
- `telegram_verified` (bool) - флаг подтверждения Telegram (устанавливается ботом в `true`)
- `phone_verified` (bool) - флаг подтверждения телефона
- `email_verified` (bool) - флаг подтверждения e-mail
- `first_name`, `last_name`, `middle_name` - личные данные
- `full_name` - полное имя (может быть сгенерировано)
- `birth_date` - дата рождения
- `email` - рабочий e-mail
- `role` - роль пользователя (`director`, `manager`, `employee`)

---

### Условия для перехода в дашборд (ФИНАЛЬНАЯ ПРОВЕРКА)

**Функция:** `finishRegistration()` в `app/register/RegisterDirectorClient.tsx`

**Условие 1:** `phone` должен быть заполнен
```typescript
if (!profile?.phone || profile.phone.trim() === "") {
  // Ошибка: номер телефона не подтвержден
  return;
}
```

**Условие 2:** `telegram_verified` должен быть `true`
```typescript
if (!profile?.telegram_verified) {
  // Ошибка: Telegram не подтвержден
  return;
}
```

**Если оба условия выполнены:**
```typescript
router.push("/dashboard/director");
```

**Важно:**
- Проверка выполняется через `/api/auth/load-profile` (обходит RLS)
- Никаких других проверок не требуется
- Если хотя бы одно условие не выполнено - редиректа нет, показывается ошибка

---

### Автоматическая проверка на шаге 4 (детали реализации)

**Файл:** `app/register/RegisterDirectorClient.tsx` → `useEffect` для `step === 4`

**Логика:**

1. **Инициализация:**
   - При переходе на шаг 4 **сразу очищаются ошибки**: `setRegisterError(null)`
   - Это предотвращает показ старых ошибок при переходе

2. **Проверка сессии:**
   - Сначала проверяется сессия через `supabase.auth.getSession()`
   - Если нет сессии → просто ждем, ошибку не показываем
   - Если есть ошибка сессии → логируем, но ошибку не показываем

3. **Запрос профиля:**
   - Используется `fetch('/api/auth/load-profile', { credentials: 'include' })`
   - **401 ошибка:** Не показывается ошибка, просто ждем (сессия еще не готова)
   - **Другие ошибки:** Логируются, но не показываются пользователю

4. **Проверка условий:**
   ```typescript
   const hasPhone = profile?.phone && profile.phone.trim() !== "";
   const isTelegramVerified = profile?.telegram_verified === true || 
                              profile?.telegram_verified === "true" ||
                              profile?.telegram_verified === 1;
   ```

5. **Отображение статуса:**
   - **Если данные готовы:**
     - `setRegisterError(null)` - убирается сообщение
     - Останавливается polling
   - **Если данные не готовы:**
     - Показывается **информационное сообщение** (синий цвет): "Ожидание подтверждения данных Telegram..."
     - Сообщение показывается только один раз (проверка: `if (!registerError || registerError !== "Ожидание...")`)

6. **Тайминги:**
   - Первая проверка через **1.5 секунды** после перехода на шаг 4
   - Затем проверка каждые **2 секунды**

**Остановка polling:**
- Polling останавливается когда данные готовы (`clearInterval`)
- Или при размонтировании компонента/смене шага (cleanup функция)

**Визуальное отображение ошибок:**
- **Информационные сообщения** (ожидание): Синий цвет, без иконки AlertCircle
- **Ошибки** (реальные проблемы): Красный цвет, с иконкой AlertCircle
- Используется `cn()` для условного применения классов

---

## 🎨 ПРАВИЛА РАБОТЫ С ТЕМАМИ (Light/Dark)

### Общие правила

1. **Всегда используй токены дизайн-системы:**
   - `bg-background` вместо хардкода цветов
   - `bg-card` для карточек и модалок
   - `text-foreground` для основного текста
   - `text-muted-foreground` для вторичного текста
   - `border-border` для границ

2. **Темная тема работает автоматически:**
   - Через класс `dark` на `<html>` элементе
   - Управляется через `ThemeProvider` (next-themes)
   - Токены автоматически адаптируются

3. **Проверка темы:**
   - `ThemeProvider` определяет тему из системных настроек браузера
   - Сохраняется в `localStorage` с ключом `shiftflow-theme`
   - Поддерживает `system`, `light`, `dark`

### Примеры правильного использования

```tsx
// ✅ ПРАВИЛЬНО
<div className="bg-background text-foreground">
  <Card className="bg-card border-border">
    <p className="text-muted-foreground">Текст</p>
  </Card>
</div>

// ❌ НЕПРАВИЛЬНО
<div className="bg-slate-900 text-white">
  <Card className="bg-slate-800 border-slate-700">
    <p className="text-gray-400">Текст</p>
  </Card>
</div>
```

---

## 📐 ПРАВИЛА ЦЕНТРИРОВАНИЯ КОНТЕНТА

### Глобальное правило

**На страницах БЕЗ навбара:**
- Контент центрируется между верхним и нижним краем экрана
- Используется: `flex min-h-screen items-center justify-center`

**На страницах С навбаром:**
- Контент центрируется между нижней частью навбара и нижним краем экрана
- Навбар имеет фиксированную высоту: `--navbar-height: 72px`

### Примеры

```tsx
// Страница БЕЗ навбара (регистрация, email-confirmed)
<main className="flex min-h-screen items-center justify-center bg-background px-4 py-8">
  {/* Контент */}
</main>

// Страница С навбаром (дашборд)
<main className="flex min-h-[calc(100vh-72px)] items-center justify-center bg-background px-4 py-8">
  {/* Контент */}
</main>
```

---

## 🌍 ПРАВИЛА РАБОТЫ С ПЕРЕВОДАМИ (i18n)

### Общие правила

1. **Никаких хардкодных строк:**
   - Все пользовательские тексты через `t("key")`
   - Ключи переводов в `lib/translations.ts`

2. **Поддерживаемые языки:**
   - `ru` - русский
   - `uk` - украинский (в коде может быть `ua`)
   - `en` - английский

3. **Использование:**
```tsx
import { useLanguage } from "@/components/language-provider";

const { t, language } = useLanguage();
const text = t("dashboard.welcome_text");
```

4. **Сохранение языка:**
   - В `localStorage` с ключом `wellify_locale`
   - В БД в таблице `profiles`, поле `language`
   - При смене языка обновляется и localStorage, и БД

5. **Структура ключей:**
   - Группировка по разделам: `dashboard.*`, `nav.*`, `support.*`
   - Вложенные ключи через точку: `dashboard.welcome_text`
   - Использование: `t("dashboard.welcome_text")`

---

## 🎨 ПРАВИЛА РАБОТЫ С ДИЗАЙН-СИСТЕМОЙ

### Цвета

**Использовать только токены:**
- `bg-background`, `bg-card`, `bg-muted`
- `text-foreground`, `text-muted-foreground`
- `border-border`
- `bg-primary`, `text-primary-foreground`
- `bg-accent`, `text-accent-foreground`
- `bg-destructive`, `text-destructive-foreground`

**Семантические цвета:**
- Успех: `text-[color:var(--color-success)]`
- Предупреждение: `text-[color:var(--color-warning)]`
- Ошибка: `text-[color:var(--color-danger)]`

**Запрещено:**
- Хардкод HEX цветов (`#2563eb`, `#0F172A`)
- Tailwind классы с хардкодом (`bg-slate-900`, `text-zinc-400`)
- Прямые цвета в `style={{ color: "#..." }}`

### Тени

**Использовать токены:**
- `shadow-[var(--shadow-card)]` для карточек
- `shadow-[var(--shadow-modal)]` для модалок
- `shadow-[var(--shadow-floating)]` для плавающих элементов
- `shadow-[var(--shadow-navbar)]` для навбара

### Радиусы

- Использовать стандартные Tailwind классы: `rounded-lg`, `rounded-xl`, `rounded-2xl`, `rounded-3xl`
- Или токены: `rounded-[var(--radius-lg)]`

---

## 🔧 ПРАВИЛА ИСПРАВЛЕНИЯ БАГОВ

### При исправлении бага:

1. **Прочитать этот файл** - убедиться, что понимаешь как должно работать
2. **Найти причину** - проверить логи, консоль, сетевые запросы
3. **Исправить** - внести минимальные изменения
4. **Проверить** - убедиться, что не сломал другую логику
5. **Обновить документацию** - если изменилась логика, обновить этот файл

### При добавлении новой функциональности:

1. **Добавить описание** в этот файл
2. **Следовать существующим правилам** (темы, переводы, дизайн-система)
3. **Не ломать существующую логику**

---

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

### 2024-12-14
- Добавлено описание процесса регистрации директора
- Описаны условия для перехода в дашборд
- Добавлены правила работы с темами, переводами и дизайн-системой
- **Обновлено описание шага 4:** Добавлена детальная информация об обработке ошибок (401, временные проблемы), информационных сообщениях вместо ошибок, правильной очистке ошибок при переходе на шаг 4

---

**ВАЖНО:** Этот файл должен обновляться при изменении логики системы. Все изменения в ключевых процессах должны быть задокументированы здесь.

